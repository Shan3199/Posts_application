<!DOCTYPE html>
<html>
  <head>
    <title>Posts</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%=render partial: "layouts/header"%>
    <% if notice %>
      <p class="notice d-flex justify-content-center alert alert-success"><%= notice %></p>
    <% end %>
    <%if alert%>
      <p class="alert d-flex justify-content-center alert alert-danger" class=""><%= alert %></p>
    <%end%>
    <%= yield %>
  </body>
</html>
